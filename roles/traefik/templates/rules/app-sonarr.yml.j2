http:
  routers:
    sonarr:
      rule: "Host(`sonarr.{{ domain }}`)"
      service: sonarr
  services:
    sonarr:
      loadBalancer:
        servers:
          - url: "http://sonarr.lab.home:8989"
